const API_KEY = "SUA_CHAVE_AQUI";

// Crie um arquivo chamado config.js na mesma pasta.

// Copie o conteúdo do config.example.js para o config.js.

// No arquivo config.js, substitua "COLE_A_CHAVE_AQUI" pela sua chave real da API.

// Exemplo: 
// const API_KEY = "SUA_CHAVE_REAL_AQUI";

// Salve o arquivo.

// Opcional: você pode simplesmente renomear o arquivo config.example.js para config.js e depois editar a chave.

// Lembre-se de adicionar o config.js no .gitignore para proteger sua chave e evitar que ela seja enviada ao repositório público.
